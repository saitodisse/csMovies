@model Dominio.Entidades.ConfiguracoesGerais

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>ConfiguracoesGerais</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.TamanhoMinimoArquivos)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TamanhoMinimoArquivos)
            @Html.ValidationMessageFor(model => model.TamanhoMinimoArquivos)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TiposArquivosPadrao)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TiposArquivosPadrao)
            @Html.ValidationMessageFor(model => model.TiposArquivosPadrao)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
